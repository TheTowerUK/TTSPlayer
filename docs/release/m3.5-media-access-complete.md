# M3.5 – Media Access Foundation

**Status:** **Accepted** — 2026-07-05  
**Date:** July 2026  
**Git tag:** `m3.5-media-access-complete`  
**Cycle:** `v0.4.0-dev` (Phase 4 active)

→ [Media access abstraction](../architecture/media-access-abstraction.md)  
→ [Path mapping](../architecture/path-mapping.md)  
→ [Phase 4 plan](../roadmap/m35-phase-4-plan.md)  
→ [Phase status](../deployment/m35-phase-status.md)  
→ [Active dev cycle](./v0.4.0-dev.md)

---

## Summary

M3.5 delivers a **provider-neutral media access architecture**. The client no longer assumes a single storage backend. Catalogue paths remain filesystem truth; playback and artwork resolve them at consumption boundaries through one abstraction.

The project evolved from *“play files from a specific path”* to *“resolve media from any supported provider through a single abstraction.”*

---

## Delivered

- Provider-neutral media access architecture ([spec](../architecture/media-access-abstraction.md))
- `MediaLocationResolver` with local and HTTP serving providers
- Playback integration (`PlaybackService.play()`)
- Artwork integration (`ArtworkImage` load path)
- Canonical path mapping specification ([path-mapping.md](../architecture/path-mapping.md))
- Caddy reference serving layer (`backend/caddy.config`, deployment docs)
- Comprehensive resolver and integration unit tests

---

## Architecture

```
Storage source
    ↓
Media access provider
    ↓
MediaLocationResolver
    ↓
Playback / Artwork
```

### Supported provider types

| Provider | Example catalogue path | Resolver output |
|---|---|---|
| Local filesystem | `D:\Media\Videos\a.mp4` | `file://` URI |
| Windows mapped drive | `Y:\Media\Videos\a.mp4` | `file://` on desktop; HTTP when configured |
| UNC / SMB | `\\NAS\Media\Videos\a.mp4` | `file://` on desktop; HTTP when configured |
| HTTP serving layer | (same fs path in catalogue) | `https://host/media/...` |
| NAS reference deployment | TNAS + Caddy | HTTP via configured base URL |

TNAS + Caddy is a **reference HTTP provider**, not a hard dependency of the core architecture.

---

## Not included (deferred to Phase 4+)

- HTTP catalogue loading (`CatalogService` fetch from URL)
- Runtime provider selection UI
- Startup / network refactor beyond resolver registration
- Scanner changes
- Android / iOS smoke builds on home Wi‑Fi

---

## Exit criteria before Phase 4 — satisfied

All mandatory exit criteria met 2026-07-05. → [M3.5 Final Acceptance](#m35-final-acceptance)

### 1. Windows regression — **PASS**

→ Full checklist: [Windows Regression Checklist](#windows-regression-checklist) below.

### 2. TNAS validation — complete (HTTP :8443)

Reference provider validated 2026-07-05 — [phase status](../deployment/m35-phase-status.md#tnas-reference-provider-validation--2026-07-05):

- [x] `GET /catalog.json` → **200**
- [x] `/media/...` Range → **206** + `Content-Range` + `Accept-Ranges`
- [ ] HTTPS / `tls internal` (deferred — Windows TLS handshake failure)
- [ ] HTTP playback smoke test from Flutter (Phase 4+)

This validates the HTTP serving provider independently of Flutter catalogue loading.

---

## Windows Regression Checklist

**Verdict: PASS** — completed 2026-07-05.

- [x] App starts normally
- [x] Catalogue still loads from existing local source
- [x] Play media from `Y:\Media`
- [x] Play media from UNC path if available
- [x] Seek forwards/backwards
- [x] Pause/resume
- [x] Stop/close playback — player can be stopped or closed without error
- [x] Artwork loads for local files — **pass with observation** (see below)
- [x] Resume position unchanged
- [x] No startup/network behaviour changed

### Artwork — pass with observation (2026-07-05)

| Verdict | Detail |
|---|---|
| ✔ Functional | Most local artwork loads correctly after M3.5 resolver wiring |
| ⚠ Observed | Some media with matching image filenames in the same folder do not show artwork |

**Not an M3.5 regression.** Discovery remains in `ArtworkService`; only the load step uses `MediaLocationResolver`. Mixed success indicates the resolver works; sidecar matching is the likely cause.

→ Tracked for Phase 4.x: [Artwork discovery improvements](../roadmap/backlog-artwork-discovery-improvements.md)  
**Does not block Phase 4.**

---

## Phase 4 preview

**Phase 4 is active.** → [Phase 4 plan](../roadmap/m35-phase-4-plan.md) (sub-phases 4.1–4.5)

Implement in order: HTTP catalogue → config model → settings UI → fallback → HTTPS/TLS.

### M3.5 remains closed

Tag `m3.5-media-access-complete` @ `fa0f66a` is **immutable** except for critical bug fixes. New work is Phase 4 or backlog.

Phase 4 is **configurable media access**, not “NAS support” — the NAS is one supported provider among others.

---

## M3.5 Final Acceptance

**Date:** 2026-07-05

### Milestone summary

| Area | Status |
|---|---|
| Provider-neutral media access architecture | ✅ |
| `MediaLocationResolver` | ✅ |
| Playback integration | ✅ |
| Artwork integration | ✅ (pass with observation) |
| TNAS reference HTTP provider | ✅ (HTTP :8443 validated) |
| Path mapping | ✅ |
| Windows regression | ✅ |
| Startup behaviour | ✅ |
| Resume position | ✅ |

### Acceptance

All mandatory exit criteria have been satisfied.

Windows regression testing completed successfully.

The TNAS+Caddy reference HTTP provider has been validated using HTTP on port 8443, confirming:

- catalogue delivery
- media delivery
- HTTP Range requests (206 Partial Content)

TLS validation has been deferred as a follow-up task and is not required for M3.5 acceptance.

Artwork loading remains functional with an existing discovery observation recorded separately in the [Phase 4.x backlog](../roadmap/backlog-artwork-discovery-improvements.md).

**Status: Accepted**

Phase 4 may begin.

---

## Commits in this checkpoint

| Commit | Summary |
|---|---|
| `f5f8f3a` | Phase 1 serving layer docs and local Caddy validation |
| `21e1183` | Media access abstraction spec (Phase 2.5) |
| `ad699d9` | `MediaLocationResolver` + unit tests (Phase 3a) |
| `3b1ca0b` | Wire resolver into playback and artwork (Phase 3b) |
| `345c6cb` | Phase 3b wiring tests |

---

## Tag

```bash
git tag -a m3.5-media-access-complete -m "M3.5 complete: provider-neutral media access architecture"
git push origin m3.5-media-access-complete
```
