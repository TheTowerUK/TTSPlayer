# TTSPlayer v0.4.0-dev — Development Cycle

**Opened:** July 2026  
**Milestone:** M3.5 — Network Client Foundation  
**Platform focus:** NAS serving layer + Flutter HTTP access mode (Android/iOS smoke builds)  
**Predecessor:** [v0.3.0](./v0.3.0.md) (closed — tag `v0.3.0`)

→ [M3.5 goals](../roadmap/network-client-foundation.md)  
→ [Mobile delivery overview](../roadmap/mobile-delivery.md)

---

## Version policy

| Line | Meaning |
|---|---|
| `v0.3.0` | **Closed** — M3 Personal Media Experience (Windows desktop) |
| `v0.4.0-dev.*` | **Active development** — all work targets M3.5 until release |
| `v0.4.0` | **Future stable tag** — when M3.5 success criteria are met |

Application version in `pubspec.yaml`: **`0.4.0-dev.1+1`**

Increment the dev patch (`0.4.0-dev.2+1`, …) for checkpoint commits during the cycle. Bump to `0.4.0+1` only when M3.5 ships.

---

## Why v0.4.0 maps to M3.5

M3 delivered the Windows-first personal media shell. M3.5 is the **bridge milestone**: the same Flutter app gains HTTPS catalogue and media streaming so phones on the home network can browse and play without SMB mounts.

This is a platform-access expansion, not a content-type expansion. **M4 (Rich Media Libraries)** remains documented but **out of scope** until M3.5 closes.

---

## M3.5 objectives (this cycle)

| Area | Deliverable |
|---|---|
| NAS serving | Caddy or Nginx on TNAS serving `catalog.json` and media with range requests |
| Path resolver | Map scanner `file_path` values to HTTPS stream URLs |
| Client HTTP mode | Load catalogue from URL; stream playback over HTTPS |
| Mobile settings | NAS base URL and catalogue endpoint configuration |
| Mobile builds | Android / iOS smoke builds on home Wi‑Fi |
| Graceful degradation | Last-good catalogue retained on network failure |
| Desktop unchanged | Windows local/UNC + local scanner continue to work |

→ Full criteria: [network-client-foundation.md](../roadmap/network-client-foundation.md)

---

## Explicitly out of scope (v0.4.0-dev)

- M4 image library viewer and grid UX
- M5 music / M6 books
- App Store / Play Store release polish
- Remote control, cast, multi-device sync (M7)
- Running `indexer.py` on mobile devices
- TMDB or external metadata

---

## Suggested implementation order

1. **NAS serving layer** — static `catalog.json`, media files, HTTPS, range headers
2. **Path resolver** — deterministic URL mapping documented and tested against real paths
3. **CatalogService HTTP path** — fetch with timeout; preserve last-good catalogue on failure
4. **PlaybackService streaming** — HTTPS URLs with seek/range support
5. **Mobile settings UI** — NAS URL configuration (minimal viable)
6. **Android / iOS smoke builds** — home-network end-to-end test
7. **Diagnostics** — network vs demo vs live NAS labelling on mobile

---

## Quality gate (target for v0.4.0 stable)

| Check | Target |
|---|---|
| Phone on home Wi‑Fi loads NAS `catalog.json` over HTTPS | Required |
| Video play + seek over HTTPS | Required |
| Failed fetch preserves previous catalogue | Required |
| Windows desktop regression suite | Required |
| `flutter analyze` | Clean |
| Dart + Python tests | Passing |

---

## Checkpoint tags (suggested)

Use annotated tags at sprint boundaries, e.g.:

- `v0.4.0-dev.1` — cycle opened (this document)
- `m35-serving-layer` — NAS Caddy/Nginx deployed and verified
- `m35-http-client` — Flutter HTTP catalogue + streaming path working
- `v0.4.0` — M3.5 complete

---

## References

- [Release history](./release-history.md)
- [M3 milestone review](../roadmap/m3-milestone-review.md)
- [Roadmap](../roadmap/roadmap.md)
- [Backend Caddy config](../../backend/caddy.config) (starting point)
